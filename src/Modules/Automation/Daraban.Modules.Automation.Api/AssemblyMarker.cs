namespace Daraban.Modules.Automation.Api;

/// <summary>Referenced by each Host's Program.cs to register this assembly as an MVC
/// Application Part (Task 1.4) -- ASP.NET Core does not auto-discover controllers living
/// in a referenced-but-not-entry assembly, so every module needs one of these.</summary>
public sealed class AssemblyMarker;
